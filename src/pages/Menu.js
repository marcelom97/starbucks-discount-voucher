import React from 'react';
import { Row } from 'react-bootstrap';

export default function Menu() {
  return (
    <Row className={'d-flex justify-content-center'}>
      <h1 className={'intro-text font-weight-bold pt-5'}>Menu</h1>
    </Row>
  );
}
